training dataset: mix of ABIDE IXI
to see if there's any improvement in the prediction results.

!!!
we need to transfer the image array's dtype into float32 because tensorflow doesn't support int64.


this data_npy hosts 67*67*67 images

I've found a huge probelm in get_loss().
    (None,1) - (None,) results in a broadcasted (None,None) value, this is wrong.
    instead, we should reshape them first into some compliant shape.